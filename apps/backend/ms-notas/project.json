{
  "name": "ms-notas",
  "projectType": "application",
  "implicitDependencies": ["java-common-dto"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "mvn package -DskipTests -f apps/ms-notas/pom.xml" }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": { "command": "mvn spring-boot:run -f apps/ms-notas/pom.xml" }
    }
  }
}