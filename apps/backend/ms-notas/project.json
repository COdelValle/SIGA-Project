{
  "name": "ms-notas",
  "projectType": "application",
  "implicitDependencies": ["core-share"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/backend/ms-notas",
        "command": "./mvnw clean package -DskipTests"
      }
    },
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker buildx build --platform linux/amd64 -t $MS_GRADE_ECR:$TAG_VERSION -t $MS_GRADE_ECR:latest apps/backend/ms-notas --push"
      }
    }
  }
}