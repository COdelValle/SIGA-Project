{
  "name": "ms-usuarios-auth",
  "projectType": "application",
  "implicitDependencies": ["core-share"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/backend/ms-usuarios-auth",
        "command": "./mvnw clean package -DskipTests"
      }
    },
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker buildx build --platform linux/amd64 -t $MS_USER_ECR:$TAG_VERSION -t $MS_USER_ECR:latest apps/backend/ms-usuarios-auth --push"
      }
    }
  }
}