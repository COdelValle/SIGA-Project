{
  "name": "ms-usuarios-auth",
  "projectType": "application",
  "implicitDependencies": ["java-common-dto","java-common-security"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "mvn package -DskipTests -f apps/ms-usuarios-auth/pom.xml" }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": { "command": "mvn spring-boot:run -f apps/ms-usuarios-auth/pom.xml" }
    }
  }
}